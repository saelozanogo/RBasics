---
title: "Parcial"
author: "Santiago Lozano-Fulanito GonzÃ¡lez"
date: "21 de marzo de 2020"
output:
  pdf_document: default
  html_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

$\equ$Este examen parcial se debe realizar INDIVIDUAL parciales iguales tendrán nota de cero, y lo deben entregar antes de las 9:00 PM, deben entregar un script, es decir un .R, con las codificaciónes respectivas y debidamente comentado, es obligatorio usar las funciones vistas en las diapositiva

## 1

a. Cree 5 vectore. Uno de caracteres, uno de numeric, uno de integer y uno de complex, cada uno con más de 5 elementos

```{r}
x<- c(1.4,3.8,4.7,5.9,1.6,3.6)
y<-c("Santiago","Enrique","Lozano", "González","Fulanito")
z<- as.integer(1:6)
w<- c(1+5i,2+6i,3+7i,4+8i,5+1i,6+2i,7+3i)
```


b. Chequee si los vectores son del tipo especificado ¿Cómo lo hace en R?

```{r}
class(x)
```
```{r}
class(y)
```

```{r}
class(z)
```

```{r}
class(w)
```



c. Cree una lista de varios tipos de estructuras de datos, mínimo 4 elementos debe tener la lista

```{r}
Mat<-matrix(1:12,nrow=4,byrow=TRUE)
L<-list(12,c(34,67),Mat,1:15,list(10,11))
L
```

d. Cree una matriz de 3 filas por 4 columna con datos arreglados del 1 al 12 en forma horizontal

```{r}
M <- matrix(1:12,nrow = 3,byrow = T)
M
```


e. Cree una matriz de 3 filas y 4 columnas con números arbitrarios y con acomodación vertical

```{r}
x<-runif(12)
Ma <- matrix(x,nrow = 3,byrow = F)
Ma
```

f. Cree un vector de factores con calificaciones de estudiantes (Deficiente, insuficiente,aceptable, sobresaliente, excelente), que contenga mínimos 13 entradas distribuidas de manera aleatoria

```{r}
calificaciones <- factor(c("deficiente","excelente","insuficiente","deficiente","aceptable","suficiente","excelente","insuficiente","excelente","insuficiente","deficiente","insuficiente","insuficiente","excelente","excelente"))
calificaciones
```


g. Cree un vertor de tipo ordered con niveles de riesgo en un hospital (bajo, medio, alto) donde bajo es el menor, medio el que le sigue y alto es mayor, debe tener mínimos 8 entradas

```{r}
z <- ordered(c("alto","bajo","bajo","medio","alto","bajo","medio","bajo","medio","bajo","alto","bajo","medio","alto","bajo","medio","alto"),
levels=c("bajo","medio","alto"))
z
```


## 2

Cree la siguiente base de datos

```{r}
base11 <- read.table("base11.txt",header = T,sep = " ")
base11
```

```{r}
attach(base11)
id <- c(539,1190,457,230,117,487,217,532)
sexo <-c("female","female","female","female","female","female","female","male")
ano.de.nacimiento <- c(1975,1960,1963,1961,1980,1967,1962,1956)
fecha.de.confirmacion <- c("2020-02-23","2020-02-26","2020-02-23","2020-02-22", "2020-02-21","2020-02-23","2020-02-22","2020-02-23")
data <- data.frame("id"=id,"sexo"=sexo,"año.de.nacimiento"=ano.de.nacimiento,"fecha.de.confirmación"=fecha.de.confirmacion)
data
```

## 3

Importe la base de datos base22

```{r}
base22 <- read.table("base22.txt",header = T,sep = " ")
base22
```

a. Extraiga los id de las mujeres (sexo=="female")

```{r}
attach(base22)
id[sexo=="female"]
```


b. Extraiga las filas de la 4 a 7 y las columnas de 2 a 4

```{r}
data[4:7,2:4]
```


## 4

De la matriz

```{r}
ymatrix <- matrix(data = c(6,34,923,5,0, 112:116, 5,9,34,76,2, 545:549),nrow=5)
ymatrix
```

a. Saque el promedio de cada fila

```{r}
apply(ymatrix,MARGIN = 1,FUN = mean)
```


b. ordene cada fila de mayor a menor

```{r}
apply(ymatrix,MARGIN = 1,FUN =function(x){rev(sort(x))})
```

## 5. 

Importe la base de datos worms 

```{r}
worms <- read.table("worms.txt",header = T,dec = ".")
attach(worms)
worms
```

a. saque el promedio de los elementos de Area con respecto a Vegetation, es decir cuál es el promedio de las Areas de Grassland y así sucesivamente

```{r}
tapply(Area,Vegetation,mean)
```